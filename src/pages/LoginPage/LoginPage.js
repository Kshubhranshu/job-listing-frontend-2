import React from "react";
import Login from "../../components/Login/Login";

export default function LoginPage() {
    return (
        <div style={{ display: "flex" }}>
            <Login />
            <img src="" alt="" style={{ maxHeight: "100vh", width: "50vw" }} />
        </div>
    );
}
